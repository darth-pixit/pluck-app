# Pluks desktop app — analytics config (PUBLIC keys only).
# Copy to `.env.production` and fill in real values for release builds.
VITE_POSTHOG_KEY=phc_PLACEHOLDER
VITE_POSTHOG_HOST=https://us.i.posthog.com
VITE_SENTRY_DSN=https://PLACEHOLDER@o0.ingest.sentry.io/0

# Build-time only — never bundled. Used by @sentry/vite-plugin for source-map upload.
# Set in CI secrets, not in this file.
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=pluks
# SENTRY_PROJECT=pluks-app
