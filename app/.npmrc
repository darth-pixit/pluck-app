; @sentry/react v7 declares react peer as 15-18 only; we're on React 19.
; v8 supports React 19 but is API-incompatible — bumping is a separate task.
; Until then, allow the install to proceed (CI uses the same flag via this file).
legacy-peer-deps=true
